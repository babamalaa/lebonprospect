export const dynamic = "force-dynamic";
export const metadata = {
  title: "Espace équipe · LeBonProspect",
  robots: { index: false, follow: false },
};

export default function ClosersLayout({ children }) {
  return children;
}
