export const dynamic = "force-dynamic";
export const metadata = { title: "Kit de démarrage · LeBonProspect", robots: { index: false, follow: false } };
export default function KitLayout({ children }) { return children; }
