export const dynamic = "force-dynamic";
export const metadata = { title: "Mon espace · LeBonProspect", robots: { index: false, follow: false } };
export default function AppLayout({ children }) { return children; }
